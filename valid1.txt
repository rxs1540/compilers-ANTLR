{
    int x, y;
    float[10] arr;
    boolean flag;
    {
        int x;
        x = 5;
    }
    x = 10;
    y = x + 2;
    if (y > 5 && y < 20) {
        arr[0] = 1.0;
    }
    while (x < 20) x = x + 1;
}
